# Bug Audit: Recommender Systems From Zero

Date: 2026-07-28
Target project: `ikonushok/recommender-systems-from-zero`
Audited commit: `6ad30d7c76bebab1fa28fbce35ddf7102792f04e`

## Summary

- Verdict: `PASS_WITH_RISKS`
- Validation level / evidence level: L2 read-only audit with existing test execution
- Validation basis: read-only bug discovery from source, tests, config, existing unit test output, import/AST checks, dependency availability checks, and notebook metadata; no reproduction files or production fixes were created.
- Audit mode: `docs-vs-code` plus mandatory bug discovery
- Report type: `bug-audit`
- Scope: source modules, tests, config, notebook metadata
- Main risk: advanced edge cases are less tested than the core course path

Mandatory bug discovery found concrete candidates, but no new reproduction files were created and no production code was changed. Existing tests were run safely with bytecode writing disabled.

## Bug Candidates

| # | Candidate | Evidence strength | Contract evidence | Trigger | Location | Confidence | Reproduction status |
|---|---|---|---|---|---|---|---|
| 1 | NeuralCF negative sampling can loop forever when a user has seen all items. | direct code contradiction | Training sampler needs an available negative item or a clear skip/fail policy. | Dense fixture where a user interacted with every item in `item_to_index_`. | `src/recsys_basics/advanced/neural_cf.py:262-265`, `:316-319` | High | `NOT_REPRODUCED` |
| 2 | TwoTower negative sampling has the same all-items-seen loop risk. | direct code contradiction | Pairwise retrieval training needs a negative pool. | Dense user history after intersecting interactions with feature tables. | `src/recsys_basics/advanced/two_tower.py:334-337` | High | `NOT_REPRODUCED` |
| 3 | Package metadata omits runtime dependencies. | static config contradiction | Importable package uses pandas/numpy/scipy/sklearn and optional advanced deps. | User installs package with `pip install .` instead of installing requirements first. | `pyproject.toml:5-16`, `requirements.txt:1-13` | Medium-high | `NOT_REPRODUCED` |
| 4 | Hugging Face Amazon Reviews loader dependency is not in requirements. | product/API gap | `load_amazon_reviews_2023_frames_hf()` exists and instructs installing `datasets`. | User expects all helper functions to work after installing `requirements.txt`. | `src/recsys_basics/data.py:23-29`, `:549-560`, `requirements.txt` | Medium | `NOT_REPRODUCED` |

## Immediate Bug-Fix Batch

Use this batch if the goal is to harden the public example without broad refactoring.

| # | Severity | Candidate | Trigger | Evidence | Confidence | Status |
|---|---|---|---|---|---|---|
| 1 | `HIGH` | NeuralCF all-items-seen negative sampling hang. | Dense user-item fixture. | `neural_cf.py:262-265`, `:316-319` | High | `NOT_REPRODUCED` |
| 2 | `HIGH` | TwoTower all-items-seen negative sampling hang. | Dense feature-intersection fixture. | `two_tower.py:334-337` | High | `NOT_REPRODUCED` |
| 3 | `MEDIUM` | Package install dependencies missing from `pyproject.toml`. | Package-style install. | `pyproject.toml`, `requirements.txt` | Medium-high | `NOT_REPRODUCED` |

## Second Engineering Batch

| # | Severity | Candidate | Trigger | Evidence | Confidence | Status |
|---|---|---|---|---|---|---|
| 4 | `MEDIUM` | Optional `datasets` dependency not declared for HF loader. | Calling `load_amazon_reviews_2023_frames_hf`. | `data.py:549-560` | Medium | `NOT_REPRODUCED` |
| 5 | `LOW` | No CI evidence for tests. | Public change lands without local test run. | No `.github` workflow files found. | High as process gap | `NOT_REPRODUCED` |

## Backlog / Hardening Batch

| # | Severity | Candidate | Why backlog | Evidence | Status |
|---|---|---|---|---|---|
| 6 | `LOW` | Full notebook run-all is not automated. | Existing metadata supports prior runs; the missing piece is repeatability, not a current code bug. | Notebook metadata inspection, README run-all claim. | `NOT_REPRODUCED` |
| 7 | `LOW` | Dataset download helpers have no retry/backoff. | Acceptable for educational scripts; not a production downloader. | `data.py` download functions. | `NOT_REPRODUCED` |

## Reproduction Approval Gate

No reproduction files were created in this audit. To prove the top candidates, request approval for:

- Candidate(s) to test: NeuralCF and TwoTower all-items-seen negative sampling.
- Exact files to create or edit: `tests/test_neural_cf.py`, `tests/test_two_tower.py` or a single focused advanced edge-case test file.
- Minimal fixture: one user, two items, interactions covering both items; for TwoTower also minimal numeric user/item feature tables.
- Test command: `PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=src python -m unittest discover -s tests -v`.
- Signal that confirms the bug: current implementation hangs or must be guarded by a test timeout/harness; fixed implementation raises a clear `ValueError` or skips users with no negative pool.
- Main risk or uncertainty: the local audit environment does not have `torch`, so the test should isolate sampler logic or run in a full advanced environment.
- Project files modified so far: `no`.

## Fix Approval Gate

Not reached. Production code should not be changed until a focused reproducer is approved and the failure mode is confirmed.

## Commands Attempted

| Command | Outcome | Interpretation |
|---|---|---|
| `PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=src python3 -m unittest discover -s tests -v` | `Ran 27 tests ... OK` | Existing core and sequence tests pass. |
| Import-all script for 13 modules | `IMPORT_OK 13` | Optional dependency guards allow advanced modules to import. |
| AST parse for `src` and `tests` | `AST_OK` | No Python syntax blockers in inspected files. |
| Local dependency availability script | advanced deps missing locally: `implicit`, `lightfm`, `optuna`, `torch`, `jupyterlab` | Advanced runtime claims were not revalidated here. |
| Notebook metadata inspection | 15 notebooks have executed code-cell metadata and outputs | Supports prior run evidence but does not replace fresh run-all. |

## Recommended Order

1. Add negative-pool guard tests for NeuralCF and TwoTower.
2. Add implementation guard after reproduction: compute available negatives once and fail fast or skip impossible users.
3. Align package metadata/install docs.
4. Add Python 3.12 CI for unit tests.
5. Add optional notebook smoke validation.

## Prioritized Closure Plan

| Priority | Action | Risk addressed | Evidence needed to close |
|---|---|---|---|
| P1 | Reproduce and fix dense negative-sampling loops. | Advanced model hang. | Regression tests pass red-to-green. |
| P1 | Make dependency install contract explicit. | User onboarding failure. | Clean install proof. |
| P2 | Add CI. | Regression prevention. | Green public workflow. |

## Evidence Log

- Files inspected: `src/recsys_basics/advanced/neural_cf.py`, `src/recsys_basics/advanced/two_tower.py`, `src/recsys_basics/data.py`, `pyproject.toml`, `requirements.txt`, `tests/**`, notebook metadata.
- Commands run: unittest, import-all, AST parse, notebook metadata inspection, local dependency version check.
- Command outcomes: existing tests passed; imports and AST passed; advanced deps not present locally.
- Candidates not reproduced: all candidates remain `NOT_REPRODUCED`; no new test files were created.
- Residual risk: top candidates are high-confidence static findings but still need approved focused reproducer before production fixes.
